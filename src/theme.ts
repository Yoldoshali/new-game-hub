const theme = {
    config: {
      initialColorMode: 'dark',
      useSystemColorMode: false,
    },
    // Define other theme properties like colors, fonts, etc. here if necessary
  };

export default theme;
